# -*- mode: snippet -*-
# name: pytest_parametrize_assert
# --
@pytest.mark.parametrize("$2", ((${2:EXAMPLES}), ($2), ($2)))
def test_${1:FUNCTION_NAME}(${2:ARGUMENTS}) -> None:
    x, y = True, False
    assert x == y
$0
