# -*- mode: snippet -*-
# name: header
# key: <he
# contributor: Karsten Beismann
# --
#!/usr/bin/env python3 > REMOVE IF UNNECESSARY.
# -*- coding: utf-8 -*-
"""${1:TITLE}.

:project: ${2:PROJECT}
:author: `(insert user-full-name)`
:contributors: `(insert user-full-name)`
:filename: `(buffer-name)`
:created: `(format-time-string "%Y-%m-%dT%T")`

Commentary
----------
$1.

Examples
--------
    $ python3 `(buffer-name)`

Environment
-----------
    $ conda activate env

To-do
-----
* ---

"""

# PACKAGES


import datetime
import logging
import os
import sys
import timeit
from logging import Logger
from pathlib import Path
from typing import (
    Any,
    Callable,
    Dict,
    Generic,
    Iterable,
    List,
    Mapping,
    NewType,
    Optional,
    Sequence,
    Sized,
    Tuple,
    TypeVar,
    Union
)

import dask.dataframe as dd  # type: ignore
import pandas as pd  # type: ignore


# CODE


$0
