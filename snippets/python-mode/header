# -*- mode: snippet -*-
# name: header
# --
#!/usr/bin/env python3 > REMOVE IF NOT A RUN SCRIPT!
# -*- coding: utf-8 -*-
"""${1:TITLE}.

Commentary
----------

$1.

Examples
--------

$ python3 `(buffer-name)`

Environment
-----------

$ conda activate ${3:ENV}

"""

import datetime
import itertools
import logging
import os
import sys
import timeit
from logging import Logger
from pathlib import Path
from typing import (
    Any,
    Callable,
    Dict,
    Generic,
    Iterable,
    List,
    Mapping,
    NewType,
    Optional,
    Sequence,
    Sized,
    Tuple,
    TypeVar,
    Union
)

import dask.dataframe as dd  # type: ignore
import pandas as pd  # type: ignore
import seaborn as sns  # type: ignore
import matplotlib.pyplot as plt  # type: ignore
import numpy as np  # type: ignore

$0
