# -*- mode: snippet -*-
# name: header
# key: <he
# contributor: Karsten Beismann
# --
;;; `(buffer-name)` --- ${1:TITLE}

;; Author: `(insert user-full-name)` <`(insert user-mail-address)`>
;; Maintainer: `(insert user-full-name)` <`(insert user-mail-address)`>
;; Filename: `(buffer-name)`
;; Created: `(format-time-string "%Y-%m-%dT%T")`
;; Keywords: ${2:KEYWORDS}


;;; Commentary:

${3:COMMENTARY}


;;; Change Log:

* ---


;;; Code:

$0


;;; `(buffer-name)` ends here.
